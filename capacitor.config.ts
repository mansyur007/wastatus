import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mansyur.wastatus',
  appName: 'WA Status Converter',
  webDir: 'dist'
};

export default config;
